# Swoosh Xcode Project — Generated by XcodeGen
# Run: xcodegen generate
# Produces Swoosh.xcodeproj containing:
#   • Swoosh                  (macOS menu-bar app)
#   • SwooshWidgetExtension   (macOS widget extension)
#   • SwooshiOS               (iOS companion app — thin client to swooshd)

name: Swoosh
options:
  bundleIdPrefix: ai.swoosh
  deploymentTarget:
    macOS: "26.0"
    iOS: "26.0"
  xcodeVersion: "26.0"
  groupSortPosition: top
  createIntermediateGroups: true
  defaultConfig: Debug

settings:
  base:
    SWIFT_VERSION: "6.3"
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    IPHONEOS_DEPLOYMENT_TARGET: "26.0"
    PRODUCT_BUNDLE_IDENTIFIER: ai.swoosh.$(PRODUCT_NAME:rfc1034identifier)
    MARKETING_VERSION: "1.1.4"
    CURRENT_PROJECT_VERSION: "6"
    DEVELOPMENT_TEAM: GY5597YK9P
    CODE_SIGN_STYLE: Automatic
    ENABLE_APP_SANDBOX: false

packages:
  Swoosh:
    path: .

targets:
  # ═══════════════════════════════════════════════════════════
  # Main App
  # ═══════════════════════════════════════════════════════════
  Swoosh:
    type: application
    platform: macOS
    sources:
      - path: App
    dependencies:
      - package: Swoosh
        product: SwooshUI
      - package: Swoosh
        product: SwooshKit
      - package: Swoosh
        product: SwooshSecrets
      - package: Swoosh
        product: SwooshWidgets
      # The app boots the agent runtime + HTTP server in-process via
      # SwooshDaemon.start() — there is no standalone swooshd binary.
      - package: Swoosh
        product: SwooshDaemon
      - package: Swoosh
        product: CodexBar
      - package: Swoosh
        product: CodexBarCore
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ai.swoosh.app
        INFOPLIST_FILE: App/Info.plist
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: true
        # LSUIElement: false → Mac app has a Dock icon and the Cartridge
        # dashboard window opens at launch. MenuBarExtra still shows in
        # the tray as a secondary surface.
        INFOPLIST_KEY_LSUIElement: false
        INFOPLIST_KEY_NSMainNibFile: ""
        INFOPLIST_KEY_CFBundleDisplayName: Cartridge
    entitlements:
      path: App/Swoosh.entitlements
      properties:
        com.apple.security.app-sandbox: false
        com.apple.security.application-groups:
          - group.ai.swoosh.shared

  # ═══════════════════════════════════════════════════════════
  # Widget Extension
  # ═══════════════════════════════════════════════════════════
  SwooshWidgetExtension:
    type: app-extension
    platform: macOS
    sources:
      - path: WidgetExtension
    dependencies:
      - package: Swoosh
        product: SwooshWidgets
      - package: Swoosh
        product: SwooshSecrets
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ai.swoosh.app.widgets
        GENERATE_INFOPLIST_FILE: true
        INFOPLIST_KEY_CFBundleDisplayName: Cartridge Widgets
        INFOPLIST_KEY_NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    entitlements:
      path: WidgetExtension/SwooshWidgetExtension.entitlements
      properties:
        com.apple.security.app-sandbox: false
        com.apple.security.application-groups:
          - group.ai.swoosh.shared

  # ═══════════════════════════════════════════════════════════
  # iOS Companion App
  # ═══════════════════════════════════════════════════════════
  # Thin client to swooshd running on the home Mac. Pairs once via a
  # bearer token, then issues chat/audit/approval requests over HTTP.
  # Does NOT embed SwooshKit (the kernel + actantdb supervisor are
  # macOS/Linux-only) — only SwooshClient + the iOS-safe slice of
  # SwooshUI.
  SwooshiOS:
    type: application
    platform: iOS
    sources:
      - path: Apps/SwooshiOS
    dependencies:
      - package: Swoosh
        product: SwooshClient
      - package: Swoosh
        product: SwooshWallet
      - package: Swoosh
        product: SwooshUI
      - package: Swoosh
        product: SwooshGenerativeUI
      - package: Swoosh
        product: SwooshLocalLLM
      - package: Swoosh
        product: SwooshFoundation
      - package: Swoosh
        product: SwooshModels
      - package: Swoosh
        product: SwooshSTT
      - package: Swoosh
        product: SwooshVoiceProviders
      - package: Swoosh
        product: SwooshLocalVoice
      - package: Swoosh
        product: SwooshMusic
      - package: Swoosh
        product: SwooshVision
      - package: Swoosh
        product: SwooshTranslation
      - package: Swoosh
        product: SwooshEmbeddings
      - package: Swoosh
        product: SwooshImageGen
      - package: Swoosh
        product: SwooshCapabilities
      - package: Swoosh
        product: SwooshArena
    entitlements:
      path: Apps/SwooshiOS/Swoosh.iOS.entitlements
      properties:
        # Raises the per-process address-space limit so the 3.65 GB
        # Gemma 4 E4B `.litertlm` model can be mapped on devices with
        # 6 GB+ RAM. Without this, large local models are restricted
        # to the standard ~2 GB iOS budget and fail to load.
        com.apple.developer.kernel.extended-virtual-addressing: true
        # Companion to the above — increases the jetsam memory limit
        # so inference doesn't OOM-kill mid-decode on supported devices.
        com.apple.developer.kernel.increased-memory-limit: true
    info:
      path: Apps/SwooshiOS/Info.plist
      properties:
        CFBundleDisplayName: Cartridge
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
        UILaunchScreen: {}
        UISupportedInterfaceOrientations:
          - UIInterfaceOrientationPortrait
          - UIInterfaceOrientationPortraitUpsideDown
          - UIInterfaceOrientationLandscapeLeft
          - UIInterfaceOrientationLandscapeRight
        # Required for any local-network access on iOS 14+. Without
        # NSLocalNetworkUsageDescription the user is never prompted and
        # all reaches to mac.local / 192.168.x.x silently fail.
        NSLocalNetworkUsageDescription: >-
          Cartridge connects to local game servers and the swooshd daemon
          running on your Mac.
        # Allow-list of Bonjour services we may browse. Used by the
        # future discovery flow; harmless when discovery is off.
        NSBonjourServices:
          - _swoosh._tcp
        # App Transport Security: allow plain `http://` to *.local and
        # RFC1918 addresses (the home-Wi-Fi pairing target). Public hosts
        # still require HTTPS — this is strictly weaker than the default
        # for local-network destinations, not a global exception.
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        NSFaceIDUsageDescription: >-
          Cartridge uses Face ID to protect local game project settings
          stored on this device.
        NSMicrophoneUsageDescription: >-
          Cartridge listens to your voice and turns it into game testing commands.
          Audio is processed on-device.
        NSSpeechRecognitionUsageDescription: >-
          Cartridge transcribes spoken game commands into text for hands-free testing.
        # Camera + photo-library attachments from the composer's `+` menu.
        # Without these strings the picker crashes on iOS 14+ the moment
        # the user grants the system the permission dialog.
        NSCameraUsageDescription: >-
          Cartridge lets you attach a photo from the camera to your next
          message. Images stay on your device until you send.
        NSPhotoLibraryUsageDescription: >-
          Cartridge lets you attach a photo from your library to your next
          message. Selection happens through the system picker.
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: ai.swoosh.app.ios
        TARGETED_DEVICE_FAMILY: "1,2"
        SUPPORTS_MAC_DESIGNED_FOR_IPHONE_IPAD: false
        SUPPORTS_MACCATALYST: false
        # The iOS app currently has no AppIcon asset. Once the asset
        # catalog exists, actool defaults to looking up `AppIcon` and
        # errors if it can't find one. Blank disables that lookup so the
        # bundled brand SVGs build clean. Replace with "AppIcon" when an
        # icon set is added.
        ASSETCATALOG_COMPILER_APPICON_NAME: ""

schemes:
  Swoosh:
    build:
      targets:
        Swoosh: all
        SwooshWidgetExtension: all
    run:
      config: Debug
    test:
      config: Debug
  SwooshiOS:
    build:
      targets:
        SwooshiOS: all
    run:
      config: Debug
    test:
      config: Debug
